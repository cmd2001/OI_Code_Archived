#include<iostream>
#include<cstdio>
#include<cstring>
using namespace std;
int N,now;
int main()
{
	freopen("color.in","r",stdin);
	freopen("color.out","w",stdout);
	scanf("%d",&N);
	char ch;
	ch=getchar();
	for(int a=1;a<=N;a++)
	{
		for(int b=1;b<=N;b++)
		{
			ch=getchar();
		}
		ch=getchar();
	}
	printf("233");
	fclose(stdin);
	fclose(stdout);
	return 0;
}
